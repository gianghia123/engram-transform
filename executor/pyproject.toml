[project]
name = "executor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nghia, Bui Gia", email = "nghiabuigia2006@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "wasmtime>=48.0.0",
]

[project.scripts]
executor = "executor:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
