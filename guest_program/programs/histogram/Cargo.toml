[package]
name = "histogram"
edition = "2024"

[lib]
crate-type = ["cdylib"]   # produces a standalone .wasm exporting alloc/run

[dependencies]
engram-shared = { path = "../../shared" }

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
